# 🔐 Настройка Google Authentication через Firebase

## ✅ Что было сделано

1. ✅ Установлен пакет `firebase`
2. ✅ Создана конфигурация Firebase (`src/firebase/config.js`)
3. ✅ Обновлен `auth-modal.jsx` с реальной авторизацией
4. ✅ Добавлены:
   - Индикатор загрузки при авторизации
   - Обработка ошибок с русскими сообщениями
   - Безопасное хранение конфигурации через `.env.local`

## 📋 Что нужно сделать

### Шаг 1: Создать проект Firebase (5 минут)

1. Перейти на [Firebase Console](https://console.firebase.google.com/)
2. Нажать **"Добавить проект"** или **"Add project"**
3. Ввести название (например, `art-gallery-auth`)
4. Отключить Google Analytics (необязательно для авторизации)
5. Нажать **"Создать проект"**

### Шаг 2: Зарегистрировать веб-приложение

1. В консоли Firebase нажмите на иконку **`</>`** (Web)
2. Введите название приложения (например, `Art Gallery`)
3. **Не нужно** настраивать Firebase Hosting
4. Нажмите **"Зарегистрировать приложение"**
5. **СКОПИРУЙТЕ** объект `firebaseConfig` (понадобится в Шаге 4)

Пример того, что нужно скопировать:

```javascript
const firebaseConfig = {
  apiKey: 'AIzaSyBx...',
  authDomain: 'art-gallery-12345.firebaseapp.com',
  projectId: 'art-gallery-12345',
  storageBucket: 'art-gallery-12345.appspot.com',
  messagingSenderId: '123456789',
  appId: '1:123456789:web:abc123def456',
};
```

### Шаг 3: Включить Google Authentication

1. В боковом меню выберите **"Authentication"** (🔐)
2. Нажмите **"Get Started"**
3. Перейдите на вкладку **"Sign-in method"**
4. Найдите **"Google"** в списке
5. Нажмите на **"Google"**
6. Включите переключатель **"Enable"**
7. В поле **"Project support email"** выберите ваш email
8. Нажмите **"Save"**

### Шаг 4: Настроить переменные окружения

1. В корне проекта создайте файл `.env.local` (скопируйте `.env.example`)
2. Заполните значения из `firebaseConfig` (из Шага 2):

```env
REACT_APP_FIREBASE_API_KEY=AIzaSyBx...
REACT_APP_FIREBASE_AUTH_DOMAIN=art-gallery-12345.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=art-gallery-12345
REACT_APP_FIREBASE_STORAGE_BUCKET=art-gallery-12345.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789
REACT_APP_FIREBASE_APP_ID=1:123456789:web:abc123def456
```

**⚠️ ВАЖНО:** Файл `.env.local` не должен попадать в Git! Он уже добавлен в
`.gitignore`.

### Шаг 5: Добавить домены для авторизации

1. В **Authentication** → **Settings** → **Authorized domains**
2. Убедитесь, что добавлены:
   - ✅ `localhost` (уже есть по умолчанию)
   - Добавьте ваш домен GitHub Pages:
     - Нажмите **"Add domain"**
     - Введите: `andyfullstack.github.io`
     - Нажмите **"Add"**

### Шаг 6: Запустить приложение

```bash
npm start
```

Теперь:

1. Откройте приложение в браузере
2. Нажмите на иконку **User** в header
3. Нажмите **"Увійти через Google"**
4. Выберите Google аккаунт
5. ✅ Авторизация работает!

## 🐛 Решение проблем

### Ошибка: "Firebase не налаштовано"

- Проверьте, что создан файл `.env.local` с правильными значениями
- Перезапустите `npm start` (переменные окружения загружаются при старте)

### Ошибка: "auth/configuration-not-found"

- Убедитесь, что включен Google провайдер в Firebase Console
- Проверьте, что указан support email

### Ошибка: "auth/unauthorized-domain"

- Добавьте ваш домен в **Authorized domains** (см. Шаг 5)
- Для GitHub Pages: `<username>.github.io`

### Вопросы?

См. подробную инструкцию в `FIREBASE_SETUP.md`

## 🎉 Готово!

После настройки пользователи смогут:

- ✅ Войти через Google (всплывающее окно)
- ✅ Зарегистрироваться через email/пароль
- ✅ Войти через email/пароль
- ✅ Видеть понятные ошибки на русском языке
